<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
<body>
</body>

<%
  user_id = 1
  user_hash = OpenSSL::HMAC.hexdigest(
    'sha256',
    @web_widget.hmac_token,
    user_id.to_s
  )

%>
<script>

window.1080ticSettings = {
  hideMessageBubble: false,
  // showUnreadMessagesDialog: false,
  // baseDomain: '.loca.lt',
  position: '<%= @widget_position %>',
  locale: 'en',
  useBrowserLanguage: true,
  type: '<%= @widget_type %>',
  // showPopoutButton: true,
  widgetStyle: '<%= @widget_style %>',
  darkMode: '<%= @dark_mode %>',
};

(function(d,t) {
  var BASE_URL = '';
  var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
  g.src= BASE_URL + "/packs/js/sdk.js";
  g.async = true;
  s.parentNode.insertBefore(g,s);
  g.onload=function(){
    window.1080ticSDK.run({
      websiteToken: '<%= @web_widget.website_token %>',
      baseUrl: BASE_URL
    })
  }
})(document,"script");

window.addEventListener('1080tic:ready', function() {
  console.log('1080tic:ready', window.$1080tic);
  if (window.location.search.includes('setUser')) {
    window.$1080tic.setUser('<%= user_id %>', {
      identifier_hash: '<%= user_hash %>',
      email: 'jane@example.com',
      name: 'Jane Doe',
      phone_number: ''
    });
  }
})

window.addEventListener('1080tic:error', function(e) {
  console.log('1080tic:error', e.detail)
})


window.addEventListener('1080tic:on-message', function(e) {
  console.log('1080tic:on-message', e.detail)
})

window.addEventListener('1080tic:postback', function(e) {
  console.log('1080tic:postback', e.detail)
})

window.addEventListener('1080tic:opened', function() {
  console.log('1080tic:opened')
})

window.addEventListener('1080tic:closed', function() {
  console.log('1080tic:closed')
})

window.addEventListener('1080tic:on-start-conversation', function(e) {
  console.log('1080tic:on-start-conversation', e.detail)
})
</script>
